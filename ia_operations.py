from model import DJIA_Net


def train_model(training_data, validation_data, lr, model: DJIA_Net):
    """Function to train model on single data"""


def whole_training(training_set='actions_data/train', validation_set='actions_data/verify'):
    """Function for whole model training process"""


def whole_prediction(stock_data: str, model: DJIA_Net, period: str):
    """Perform whole prediction for single stock for specified period"""
